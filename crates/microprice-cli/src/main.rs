//! `microprice` CLI.
//!
//! **Not yet implemented.** The `train`/`predict`/`evaluate`/`inspect`/
//! `benchmark` subcommands are Phase 11 of the project roadmap. This binary
//! exists only so the workspace member builds; it deliberately does not
//! pretend to be a working CLI yet.

fn main() {
    eprintln!(
        "microprice-cli: not yet implemented (Phase 11 of the project roadmap; \
         see docs/model-spec.md and the repository README for current status)."
    );
    std::process::exit(1);
}
